# Status — EnvKit

**As of:** 2026-09-19 (PT)
**Phase:** 0 — parked (docs only)
**Blocked on:** intentional — wait for ChemSpec (and preferably TeachSpec or LabRF) to settle

## Context (family sync)

Sibling stubs that un-block a future EnvKit revisit (still **not** scheduling EnvKit engineering):

- ChemSpec Workbench — MVP + Measurement Integrity polish (software UV-Vis/IR)
- TeachSpec — Phase-0 mock/calibration + USB-cam docs lock + UVC **interface** stub (no live driver)
- LabRF Monitor — Phase-1 mock RF path
- FID/NMR Playground — Phase-0 mock FID→FFT stub

## Done

- Doc pack drafted so the idea doesn’t get lost (`Project_Truth.md`, `SPEC.md`, `roadmap.md`, `status.md`)

## Next

Nothing until JARTH un-parks after core family ships. Prefer at least ChemSpec + one other family project before modality lock.

## Claims

*Deferred*. Not in active build queue. No hardware, no sensors, no compound ID.
