# ChemSpec Workbench v1 — Spec

See `phase0.md` for locked Phase 0 scope (formats, MVP, non-goals, claim classes).

**Core package:** `spectrum-core` — Spectrum, PeakPicker, Baseline, Waterfall, Ingest adapters.  
**Primary format:** CSV with column mapping; JCAMP-DX stretch.  
**Stack lean:** Python + numpy/scipy + small local UI.
